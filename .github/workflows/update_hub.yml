- name: Update Hub README
        run: |
          cd wiki
          # Lấy danh sách 10 bài mới nhất, tạo link chuẩn
          LIST=$(find . -maxdepth 1 -name "*.md" ! -name "README.md" | sed 's|./||' | sort -r | head -n 10 | while read line; do
            echo "- [$line](https://github.com/qtetech/qte-technologies-every-industrial-and-scientific-wiki/blob/main/$line)"
          done)
          
          cd ../hub
          # Kiểm tra nếu mục Latest chưa có thì thêm vào cuối file
          if ! grep -q "🆕 Latest Technical Updates:" README.md; then
            echo -e "\n## 🆕 Latest Technical Updates:\n" >> README.md
          fi

          # Dùng Python để ghi đè danh sách bài viết một cách an toàn
          python3 -c "
          import sys
          content = open('README.md').read()
          header = '## 🆕 Latest Technical Updates:'
          if header in content:
              base_content = content.split(header)[0]
              new_content = base_content + header + '\n\n' + sys.argv[1]
              with open('README.md', 'w') as f:
                  f.write(new_content)
          " "$LIST"
          
          git config --global user.name "QTE-Bot"
          git config --global user.email "bot@qtetech.com"
          git add README.md
          git commit -m "Neural Update: Sync latest wiki articles" || echo "No changes"
          git push
